<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style2.css">
    <title>Report</title>
</head>
<body>
    <div class="foot">
        <h3>Rajnikumar Tank</h3>
    </div>
    <h1>Student ID: <%=StudentName[0].studId%></h1>
    <h1>Student name: <%=StudentName[0].name%></h1>
    <table>
        <caption><h1>Annual report Card</h1></caption>
        <tr>
            <th rowspan="2">Subject Name</th>
            <th colspan="2">Prelims Marks(50)</th>
            <th colspan="2">Terminal Marks(50)</th>
            <th colspan="2">Final Marks(100)</th>
            <th rowspan="2">Total Marks(200)</th>
        </tr>
        <tr>
            <th>Practical(20)</th>
            <th>Theory(30)</th>
            <th>Practical(20)</th>
            <th>Theory(30)</th>
            <th>Practical(30)</th>
            <th>Theory(70)</th> 
        </tr>
        <%var i=0,totalAll=0; for(i=0;i<prelims.length;i++){ %>
        <tr>
            <td><%=prelims[i].name%></td>
            <td><%=prelims[i].obtainPrecticalMarks%></td>
            <td><%=prelims[i].obtainTheoryMarks%></td>
            <td><%=terminal[i].obtainPrecticalMarks%></td>
            <td><%=terminal[i].obtainTheoryMarks%></td>
            <td><%=final[i].obtainPrecticalMarks%></td>
            <td><%=final[i].obtainTheoryMarks%></td>
            <td><%= total= prelims[i].obtainPrecticalMarks + prelims[i].obtainTheoryMarks + terminal[i].obtainPrecticalMarks + terminal[i].obtainTheoryMarks + final[i].obtainPrecticalMarks + final[i].obtainTheoryMarks;  %></td>
        </tr>
        <% totalAll+=total }%>
        <tr>
        </tr>
    </table>
    <div class="foot">
        <h3>Obtain Marks:<%= totalAll %>/1200</h3>
        <h3>Percentage:<%= (totalAll/12).toFixed(2) %>%</h3>
        <h3>Attendence:<%=((final[i-1].presentDays/91)*100).toFixed(2)%>%</h3>
    </div> 
    <a href="/studentDetails/result">Go to All Student</a>
</body>
</html>